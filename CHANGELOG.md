# Changelog

## 0.1.1

- Add a configurable token-budget recommendation with a target and planning range for each model and reasoning effort.

## 0.1.0

- Add a local, explainable `codex-auto recommend` CLI with human and JSON output.
- Add a configurable model registry and policy, with API price provenance.
- Add `--prompt-file` for UTF-8 plain text task prompts.
- Add a reusable `AGENTS.md` template and repository `model-router` Skill.
- Add tests and documentation for the recommendation-only milestone.
